import 'package:flutter/material.dart';

class Jumpingastronaut extends StatelessWidget {
  const Jumpingastronaut({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 70,
      height: 70,
      child: Image.asset('images/astronaut_jumping.png'),
    );
  }
}
